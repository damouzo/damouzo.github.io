source "https://rubygems.org"

# Mirrors the exact gem versions GitHub Pages uses, so the local
# preview (`bundle exec jekyll serve`) matches the deployed site.
gem "github-pages", group: :jekyll_plugins